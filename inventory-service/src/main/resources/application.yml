spring:
  application:
    name: INVENTORY-SERVICE
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
    show-sql: true
server:
  port: 8082
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true